import { Button } from '@chakra-ui/button';

export const CreateSnippet = () => {
  return (
    <Button p={8} mr={8} display={['none', 'none', 'flex']} colorScheme="pink">
      Create Snippet
    </Button>
  );
};
